Run these commands sequentially and report any issues:

1. `cargo check --workspace`
2. `cargo clippy --workspace -- -D warnings`
3. `cargo test --workspace`

If any step fails, fix the issues before proceeding to the next step. Report a summary of results when done.